tasks:
  - init: cd frontend && npm install
    command: npm install -g @angular/cli
  - command: alias frontend='cd /workspace/reservy/frontend && npm start'
  - command: alias backend='cd /workspace/reservy && php -S localhost:8080'